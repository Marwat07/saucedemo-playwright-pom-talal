
## Features
- Playwright with TypeScript
- Page Object Model (POM) design pattern
- UI Tests: Valid login + Full checkout flow on https://www.saucedemo.com/
- API Test: OpenWeatherMap current weather for Islamabad (assert 200 OK + temperature)

For API test: Add your OpenWeather API key to `.env` as `API_KEY=your_key`
